package com.tokoagar.inventori.repository;

import com.tokoagar.inventori.entity.Penyuplai;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PenyuplaiRepository extends JpaRepository<Penyuplai, Long> {
}
